{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": "hanko migrate up --config /etc/hanko/config.yaml",
    "startCommand": "hanko serve all --config /etc/hanko/config.yaml"
  }
}
