# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.bin

# Environment files
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Temporary files
tmp/
temp/
*.tmp

# Wrangler
.wrangler/
.dev.vars

# Separate repository
arduino-compile-server/

# Claude Code local artifacts (個人ごとのBash許可リスト + scheduled_tasks lock 等)
.claude/

# Internal docs — Public 公開対象外 (history rewrite 2026-05-11 Session 100 emergency pivot 適用)
# prompt/ 全体: 元々プロンプト準備用作業フォルダ、プロダクト構成外
#   - maintenance/: handover / plans / bugs / investigations (内部運用 docs)
#   - archive/: 過去 session 作業記録 (2024-09 〜 2026-04 historical work logs)
#   - logs/: session_state (decisions_log / current_work / context_quick_ref / _CRITICAL_DEFINITIONS)
#   - guides/: ローカル user 用 program-write guide (Public release docs/ で代替)
#   - image/: AI 会話用一時 screenshot (定期削除)
# CLAUDE.md: 内部 instruction file (settled decisions / target_users / Takeda strategic axis)
# docs/dev/deployment.md: 内部 deploy 手順 (/opt/digicode + ssh ml30 等の internal infra path)
# 全件ローカル保持 (本リポジトリ管理外、Public 観点 sensitivity)。
prompt/
CLAUDE.md
docs/dev/deployment.md
